namespace Bannerlord.GameMaster.Cultures.Names
{
    /// <summary>
    /// Identifies the type of name data for cache lookup and default fallback routing
    /// </summary>
    public enum NameType
    {
        MaleHeroNames,
        FemaleHeroNames,
        HeroSuffixes,
        ClanNames,
        KingdomNames,
        FactionPrefixes
    }
}
